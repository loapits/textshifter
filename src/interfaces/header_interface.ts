export interface IHeaderContainer {
  logotext: string
  toggleTheme: () => void
}

export interface IHeader {
  logotext: string
  toggleTheme: () => void
}

export interface ILogo {
  logotext: string
}